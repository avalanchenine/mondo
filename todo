Core:
  setup more config stuff that passes to modules via init. 
    (e.g. http server runs on x ports, the pidfile is here)
  
Modules:
  abstract out proc monitoring and net monitoring. Let the modules inherit from them.
